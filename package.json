{
  "name": "gmat-quant-snake",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.23.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "lucide-react": "^0.300.0"
  },
  "scripts": {
    "start": "node scripts/lint-exams.js && node copy-assets.js && esbuild index.tsx --bundle --format=esm --servedir=dist --outfile=dist/index.js --watch --loader:.tsx=tsx --loader:.ts=ts",
    "build": "node scripts/lint-exams.js && node clean.js && node copy-assets.js && esbuild index.tsx --bundle --splitting --format=esm --minify --sourcemap --outdir=dist --target=es2020 --loader:.tsx=tsx --loader:.ts=ts --resolve-extensions=.tsx,.ts,.js --external:react --external:react-dom/* --external:lucide-react"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}